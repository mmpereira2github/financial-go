plugins {
    id 'com.github.blindpirate.gogradle' version '0.11.4'
}

golang {
    // packagePath = 'github.com/financial' // go import path of project to be built, NOT local file system path!
    packagePath = 'financial'
}

dependencies {
    golang {
    }
}